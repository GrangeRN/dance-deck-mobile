import { ScreenPlaceholder } from "@/components/common/ScreenPlaceholder";

export default function UploadScreen() {
  return <ScreenPlaceholder title="Upload Program" subtitle="Camera, photo, PDF, or text" />;
}
