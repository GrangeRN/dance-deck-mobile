import { ScreenPlaceholder } from "@/components/common/ScreenPlaceholder";

export default function SettingsScreen() {
  return <ScreenPlaceholder title="Settings" subtitle="Notifications, offline, account" />;
}
