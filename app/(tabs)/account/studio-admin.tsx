import { ScreenPlaceholder } from "@/components/common/ScreenPlaceholder";

export default function StudioAdminScreen() {
  return <ScreenPlaceholder title="Studio Admin" subtitle="Roster, members, events, settings" />;
}
