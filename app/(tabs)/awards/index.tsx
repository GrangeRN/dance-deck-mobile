import { ScreenPlaceholder } from "@/components/common/ScreenPlaceholder";
import { Trophy } from "lucide-react-native";

export default function AwardsScreen() {
  return (
    <ScreenPlaceholder
      icon={Trophy}
      title="Awards"
      subtitle="Track adjudications, placements, and titles"
    />
  );
}
