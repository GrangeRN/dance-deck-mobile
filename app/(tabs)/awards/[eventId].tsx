import { ScreenPlaceholder } from "@/components/common/ScreenPlaceholder";
import { Trophy } from "lucide-react-native";

export default function AwardEntryScreen() {
  return (
    <ScreenPlaceholder
      icon={Trophy}
      title="Award Entry"
      subtitle="Adjudications, specialty, overalls"
    />
  );
}
