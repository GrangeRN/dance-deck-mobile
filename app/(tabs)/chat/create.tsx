import { ScreenPlaceholder } from "@/components/common/ScreenPlaceholder";

export default function CreateChannelScreen() {
  return <ScreenPlaceholder title="New Channel" subtitle="Create channel and add members" />;
}
